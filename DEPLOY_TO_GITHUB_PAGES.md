# Инструкция по деплою исправлений на GitHub Pages

## 📋 Текущая ситуация

✅ **Локально**: Все файлы исправлены (UTF-8 без BOM)  
❌ **GitHub Pages**: Старая версия с крякозябрами  
⚠️ **Git**: Изменения не закоммичены

## 🚀 Шаги для деплоя

### 1. Проверка удалённого репозитория

Если удалённый репозиторий не настроен, добавьте его:

```bash
git remote add origin https://github.com/pustem754-hash/zelenaya-dolina.git
```

Или проверьте текущий remote:
```bash
git remote -v
```

### 2. Добавление всех исправленных файлов

```bash
# Добавить все исправленные файлы
git add public/

# Или добавить все изменения
git add .
```

### 3. Создание коммита

```bash
git commit -m "Исправление кодировки: удаление BOM, сохранение в UTF-8 без BOM"
```

### 4. Отправка на GitHub

```bash
# Если это первый push или нужно обновить master
git push origin master

# Или если используете main ветку
git push origin main
```

### 5. Проверка настроек GitHub Pages

1. Перейдите в репозиторий: https://github.com/pustem754-hash/zelenaya-dolina
2. Settings → Pages
3. Убедитесь, что:
   - **Source**: `master` (или `main`) branch
   - **Folder**: `/ (root)` или `/public` (в зависимости от структуры)

### 6. Ожидание деплоя

GitHub Pages обычно обновляется в течение 1-2 минут после push.

## ⚠️ Важно

Если GitHub Pages настроен на папку `/public`, убедитесь, что все файлы находятся в этой папке.

Если настроен на корень (`/`), возможно нужно:
- Переместить файлы из `public/` в корень
- Или изменить настройки GitHub Pages на папку `/public`

## 🔍 Проверка после деплоя

1. Откройте: https://pustem754-hash.github.io/zelenaya-dolina/
2. Обновите страницу (Ctrl+F5 или Cmd+Shift+R)
3. Проверьте, что крякозябры исчезли

## 📝 Альтернативный вариант (если нужна папка /public)

Если GitHub Pages настроен на корень, но файлы в `/public`:

1. Создайте файл `.nojekyll` в корне (если используете Jekyll)
2. Или настройте GitHub Pages на папку `/public` в Settings

---

**После выполнения этих шагов кодировка будет исправлена на GitHub Pages!** ✅

