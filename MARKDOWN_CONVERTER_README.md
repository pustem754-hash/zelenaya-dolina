# Markdown to HTML Converter

Функция для преобразования Markdown разметки в HTML.

## 📦 Файлы

- `markdown-to-html-converter.js` - Основная функция с примерами
- `index.js` - Версия для модулей Node.js
- `test-markdown.html` - Интерактивная страница с тестами (открыть в браузере)

## 🚀 Использование

### В браузере:

```html
<script src="markdown-to-html-converter.js"></script>
<script>
    const html = markdownToHTML('# Hello World');
    console.log(html); // <h1>Hello World</h1>
</script>
```

### В Node.js:

```javascript
const { markdownToHTML } = require('./markdown-to-html-converter.js');

const html = markdownToHTML('# Hello World');
console.log(html); // <h1>Hello World</h1>
```

## ✨ Поддерживаемые элементы

### Заголовки
```markdown
# H1
## H2
### H3
#### H4
##### H5
###### H6
```

### Форматирование текста
```markdown
**жирный** или __жирный__
*курсив* или _курсив_
~~зачёркнутый~~
`inline code`
```

### Ссылки и изображения
```markdown
[Текст ссылки](https://example.com)
![Alt текст](image.png)
```

### Списки
```markdown
- Неупорядоченный список
- Элемент 2

1. Упорядоченный список
2. Элемент 2
```

### Блоки кода
````markdown
```javascript
const x = 1;
console.log(x);
```
````

### Цитаты
```markdown
> Это цитата
```

### Горизонтальные линии
```markdown
---
***
___
```

### Параграфы
Пустая строка между текстом создаёт параграф:
```markdown
Первый параграф

Второй параграф
```

## 🧪 Запуск тестов

### Вариант 1: В браузере
Откройте файл `test-markdown.html` в браузере

### Вариант 2: Консоль (примеры)
```bash
node markdown-to-html-converter.js
```

## 🔒 Безопасность

Функция автоматически экранирует HTML теги для защиты от XSS:

```javascript
markdownToHTML('<script>alert("XSS")</script>')
// Результат: <p>&lt;script&gt;alert(&quot;XSS&quot;)&lt;/script&gt;</p>
```

## 📝 Примеры

```javascript
// Заголовок
markdownToHTML('# My Title')
// → <h1>My Title</h1>

// Текст с форматированием
markdownToHTML('This is **bold** and *italic*')
// → <p>This is <strong>bold</strong> and <em>italic</em></p>

// Ссылка
markdownToHTML('[Google](https://google.com)')
// → <p><a href="https://google.com">Google</a></p>

// Список
markdownToHTML('- Item 1\n- Item 2')
// → <ul>
//   <li>Item 1</li>
//   <li>Item 2</li>
//   </ul>
```

## ⚙️ Технические детали

Функция обрабатывает markdown в следующем порядке:
1. Сохранение блоков и инлайн кода
2. Экранирование HTML
3. Восстановление кода
4. Заголовки
5. Горизонтальные линии
6. Списки
7. Цитаты
8. Изображения
9. Ссылки
10. Форматирование текста (жирный, курсив, зачёркнутый)
11. Переносы строк
12. Параграфы

## 📄 Лицензия

ISC












