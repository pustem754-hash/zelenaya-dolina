# ⚡ Быстрый старт - HomeScreen Component

## 🎯 Что создано

✅ **HomeScreen.jsx** - полностью функциональный React-компонент главного экрана  
✅ **Tailwind CSS** - современная стилизация  
✅ **React Hooks** - useState для управления состоянием  
✅ **Адаптивный дизайн** - работает на всех устройствах  

---

## 📁 Расположение файлов

```
/workspace/src/
├── components/
│   ├── HomeScreen.jsx              ⭐ Главный компонент
│   ├── HomeScreen.example.jsx      📝 Примеры
│   └── README.md                   📖 Документация
├── index.jsx                       🚀 Точка входа
├── index.css                       🎨 Tailwind стили
├── tailwind.config.js              ⚙️ Конфигурация
└── package.json                    📦 Зависимости
```

---

## 🚀 Как использовать (3 шага)

### Вариант А: Новый проект Create React App

```bash
# 1. Создайте проект
npx create-react-app my-app
cd my-app

# 2. Установите Tailwind
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 3. Скопируйте файлы
cp /workspace/src/components/HomeScreen.jsx ./src/components/
cp /workspace/src/index.css ./src/
cp /workspace/src/tailwind.config.js ./

# 4. Обновите src/index.js
import './index.css';
import HomeScreen from './components/HomeScreen';

function App() {
  return <HomeScreen />;
}

# 5. Запустите
npm start
```

### Вариант Б: Готовая структура

```bash
# 1. Перейдите в директорию
cd /workspace/src

# 2. Установите зависимости
npm install

# 3. Запустите
npm start
```

---

## 📦 Необходимые зависимости

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.32",
    "autoprefixer": "^10.4.16"
  }
}
```

Установка:
```bash
npm install react react-dom react-scripts
npm install -D tailwindcss postcss autoprefixer
```

---

## ⚙️ Настройка Tailwind CSS

### 1. tailwind.config.js

```javascript
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

### 2. src/index.css

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### 3. Импорт в src/index.js

```javascript
import './index.css';
```

---

## 💻 Код для интеграции

### Базовое использование

```jsx
import HomeScreen from './components/HomeScreen';

function App() {
  return <HomeScreen />;
}

export default App;
```

### С React Router

```jsx
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import HomeScreen from './components/HomeScreen';

function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<HomeScreen />} />
      </Routes>
    </Router>
  );
}
```

---

## 🎨 Что включено в компонент

### ✅ Структура

- **Шапка**: Приветствие с именем пользователя
- **Ключевая информация**: 3 карточки (оплата, заявки, уведомления)
- **Быстрые действия**: 4 кнопки (показания, оплата, заявка, квитанция)
- **Уведомления**: Список последних 2 уведомлений
- **Футер**: Контакты диспетчерской с кликабельным телефоном

### ✅ Интерактивность

- Клик по уведомлению → отметка как прочитанное
- Клик по карточкам → навигация к разделам
- Клик по кнопкам → быстрые действия
- Hover эффекты на всех элементах

### ✅ Адаптивность

- Mobile: 1-2 колонки
- Tablet: 2-3 колонки
- Desktop: 3-4 колонки

---

## 🎯 Данные компонента

```javascript
// Пользователь
const userData = {
  firstName: "Иван",
  lastName: "Иванов"
};

// Статистика
const mainStats = {
  amountDue: 5234,          // Сумма к оплате
  dueDate: "10 мая",        // Срок
  activeRequests: 3,        // Заявки
  newNotifications: 2       // Уведомления
};

// Действия
const quickActions = [
  { id: 1, icon: "💧", label: "Передать показания", route: "/counters" },
  { id: 2, icon: "🔋", label: "Оплатить сейчас", route: "/payments" },
  { id: 3, icon: "📢", label: "Создать заявку", route: "/request" },
  { id: 4, icon: "📄", label: "Скачать квитанцию", route: "/receipts" }
];

// Уведомления
const notifications = [
  {
    id: 1,
    type: "warning",
    icon: "⚡",
    title: "Плановая проверка электрощитовой",
    date: "25 апреля",
    isNew: true,
    details: "Дома: 1, 2, 3. Время: 9:00-18:00"
  }
];
```

---

## 🔧 Основные функции

### handleNotificationClick

Отмечает уведомление как прочитанное:

```javascript
const handleNotificationClick = (notificationId) => {
  setNotifications(notifications.map(notification => 
    notification.id === notificationId 
      ? { ...notification, isNew: false }
      : notification
  ));
};
```

### handleQuickAction

Обрабатывает быстрые действия:

```javascript
const handleQuickAction = (route) => {
  console.log('Переход к:', route);
  // navigate(route); // С React Router
};
```

### handleStatClick

Обрабатывает клики по статистике:

```javascript
const handleStatClick = (statType) => {
  // Навигация к соответствующему разделу
};
```

---

## 🎨 Цветовая схема

```
Основной:    emerald-500 (#10b981) - зеленый
Темный:      emerald-600 (#059669)
Текст:       emerald-700 (#047857)

Акценты:
  Синий:     blue-500 (#3b82f6)
  Оранжевый: orange-500 (#f97316)

Нейтральные:
  Фон:       gray-50 (#f9fafb)
  Карточки:  white (#ffffff)
  Текст:     gray-800 (#1f2937)
```

---

## 🐛 Решение проблем

### Tailwind не работает?

```bash
# 1. Проверьте tailwind.config.js
# content: ["./src/**/*.{js,jsx,ts,tsx}"]

# 2. Проверьте импорт в index.js
# import './index.css';

# 3. Перезапустите
npm start
```

### Компонент не рендерится?

```bash
# 1. Проверьте консоль браузера (F12)
# 2. Проверьте импорт
import HomeScreen from './components/HomeScreen';

# 3. Проверьте зависимости
npm install react react-dom
```

### Порт 3000 занят?

```bash
# Используйте другой порт
PORT=3001 npm start
```

---

## 📖 Документация

**Полная документация**: `/workspace/src/components/README.md`  
**Инструкция по установке**: `/workspace/REACT_SETUP.md`  
**Подробная документация**: `/workspace/HOMESCREEN_COMPONENT.md`  

---

## ✅ Чеклист готовности

- [ ] Node.js установлен (v14+)
- [ ] Create React App создан
- [ ] Tailwind CSS настроен
- [ ] HomeScreen.jsx скопирован
- [ ] index.css содержит @tailwind директивы
- [ ] npm start запускается
- [ ] Компонент отображается
- [ ] Стили применяются
- [ ] Клики работают

---

## 📞 Контакты

**Диспетчерская служба УК "Зелёная долина"**  
📞 [8 (960) 072-03-21](tel:+79600720321)  
⏰ Режим работы: 24/7

---

## 🎉 Готово!

Компонент **HomeScreen.jsx** полностью готов к использованию.

Просто скопируйте файл в ваш проект и следуйте инструкциям выше.

---

**Версия**: 1.0.0  
**Дата**: Декабрь 2024  
**Технологии**: React 18+, Tailwind CSS 3+

© 2024 УК "Зелёная Долина"
