# УК "Зелёная Долина" - React Native Приложение

## 📱 Описание

Мобильное приложение для управления ЖК с интеграцией всех систем:
- **VSK Desk** - система заявок
- **PAL Gate** - управление шлагбаумами через SMS
- **Телеком Летай** - видеонаблюдение
- **ЕРЦ Форма 4.0** - выгрузка показаний счетчиков
- **Платежи** - загрузка скриншотов из банков
- **SMS-вход** - только для номеров из базы

## 🚀 Установка и запуск

### Требования
- Node.js 18+
- npm или yarn
- Expo CLI

### Установка зависимостей
```bash
npm install
# или
yarn install
```

### Запуск приложения
```bash
# iOS
npm run ios

# Android
npm run android

# Web
npm run web
```

## 📁 Структура проекта

```
src/
├── api/              # API интеграции всех систем
│   └── mainScreen.ts # Загрузка данных главного экрана
├── screens/          # Экраны приложения
│   └── MainScreen.tsx # Главный экран
├── store/            # Zustand хранилища
│   └── mainScreenStore.ts
├── types/            # TypeScript типы
│   └── api.ts        # Типы данных API
└── navigation/       # React Navigation
    └── AppNavigator.tsx
```

## 🔌 API Интеграция

### Настройка API URL

Создайте файл `.env`:
```
EXPO_PUBLIC_API_URL=https://api.zelenayadolina.ru
```

### Эндпоинты

- `GET /api/main-screen` - данные главного экрана
- `POST /api/gate/open` - открытие шлагбаума
- `GET /api/cameras/{id}/stream` - поток камеры

## 📊 Данные главного экрана

Главный экран получает данные из всех интегрированных систем и отображает:
- Статистику платежей
- Активные заявки из VSK Desk
- Уведомления из всех систем
- Статус шлагбаума PAL Gate
- Статус камер Телеком Летай
- Информацию о счетчиках ЕРЦ

## 🔄 Обновление данных

Данные обновляются автоматически каждые 30 секунд через React Query.
Также поддерживается pull-to-refresh для ручного обновления.

## 📝 Лицензия

© 2024 УК "Зелёная Долина". Все права защищены.

