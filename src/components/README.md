# HomeScreen Component - Документация

Компонент главного экрана для приложения УК "Зелёная долина"

## 📋 Описание

`HomeScreen` — это полнофункциональный React-компонент, созданный с использованием функциональных компонентов, хуков useState и стилизованный с помощью Tailwind CSS.

## ✨ Возможности

- ✅ Приветствие пользователя с именем
- ✅ Три интерактивные карточки с ключевыми метриками
- ✅ Четыре кнопки быстрых действий
- ✅ Список последних уведомлений (максимум 2)
- ✅ Отметка уведомлений как прочитанных при клике
- ✅ Футер с кликабельным номером телефона диспетчерской
- ✅ Полностью адаптивный дизайн (mobile-first)
- ✅ Плавные анимации и hover-эффекты

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установите React и ReactDOM (если еще не установлены)
npm install react react-dom

# Установите Tailwind CSS
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

### 2. Настройка Tailwind CSS

Создайте файл `tailwind.config.js`:

```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

Добавьте в `src/index.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### 3. Использование компонента

```jsx
import React from 'react';
import HomeScreen from './components/HomeScreen';

function App() {
  return <HomeScreen />;
}

export default App;
```

## 📊 Структура данных

### userData (Данные пользователя)

```javascript
{
  firstName: "Иван",
  lastName: "Иванов"
}
```

### mainStats (Ключевые метрики)

```javascript
{
  amountDue: 5234,        // Сумма к оплате (рубли)
  dueDate: "10 мая",      // Срок оплаты
  activeRequests: 3,      // Количество активных заявок
  newNotifications: 2     // Количество новых уведомлений
}
```

### quickActions (Быстрые действия)

```javascript
[
  { id: 1, icon: "💧", label: "Передать показания", route: "/counters" },
  { id: 2, icon: "🔋", label: "Оплатить сейчас", route: "/payments" },
  { id: 3, icon: "📢", label: "Создать заявку", route: "/request" },
  { id: 4, icon: "📄", label: "Скачать квитанцию", route: "/receipts" }
]
```

### notifications (Уведомления)

```javascript
[
  {
    id: 1,
    type: "warning",
    icon: "⚡",
    title: "Плановая проверка электрощитовой",
    date: "25 апреля",
    isNew: true,
    details: "Дома: 1, 2, 3. Время: 9:00-18:00"
  },
  {
    id: 2,
    type: "info",
    icon: "🚿",
    title: "Отключение горячей воды",
    date: "28 апреля",
    isNew: true,
    details: "Причина: Профилактические работы. Время: 10:00-16:00"
  }
]
```

## 🎨 Использованные Tailwind классы

### Цветовая схема

- **Основной цвет (зеленый)**: `emerald-500`, `emerald-600`, `emerald-700`
- **Фон**: `bg-white`, `bg-gray-50`, `bg-gray-100`
- **Текст**: `text-gray-600`, `text-gray-700`, `text-gray-800`

### Компоненты

- **Карточки**: `rounded-xl`, `shadow-md`, `hover:shadow-xl`
- **Сетка**: `grid`, `grid-cols-2`, `md:grid-cols-3`, `md:grid-cols-4`
- **Отступы**: `p-4`, `p-5`, `p-6`, `px-6`, `py-8`
- **Анимации**: `transition-all`, `duration-300`, `hover:scale-105`

## 🔧 Интерактивность

### Обработчики событий

#### handleNotificationClick(notificationId)

Отмечает уведомление как прочитанное при клике:

```javascript
const handleNotificationClick = (notificationId) => {
  setNotifications(notifications.map(notification => 
    notification.id === notificationId 
      ? { ...notification, isNew: false }
      : notification
  ));
};
```

#### handleQuickAction(route)

Обрабатывает клик по кнопке быстрого действия:

```javascript
const handleQuickAction = (route) => {
  console.log('Переход к:', route);
  // В реальном приложении: navigate(route);
};
```

#### handleStatClick(statType)

Обрабатывает клик по карточке статистики:

```javascript
const handleStatClick = (statType) => {
  console.log('Клик по статистике:', statType);
  // Навигация к соответствующему разделу
};
```

## 🔗 Интеграция с React Router

Для добавления навигации установите React Router:

```bash
npm install react-router-dom
```

Обновите обработчики:

```javascript
import { useNavigate } from 'react-router-dom';

const HomeScreen = () => {
  const navigate = useNavigate();
  
  const handleQuickAction = (route) => {
    navigate(route);
  };
  
  // ... остальной код
};
```

## 📱 Адаптивный дизайн

Компонент оптимизирован для всех размеров экранов:

- **Mobile** (< 768px): 1-2 колонки
- **Tablet** (768px - 1024px): 2-3 колонки
- **Desktop** (> 1024px): 3-4 колонки

## 🎯 Кастомизация

### Изменение цветовой схемы

Замените `emerald` на другой цвет Tailwind:

```jsx
// Было
className="bg-emerald-500"

// Стало (например, синий)
className="bg-blue-500"
```

### Добавление новых быстрых действий

```javascript
const [quickActions] = useState([
  // ... существующие действия
  { id: 5, icon: "📞", label: "Позвонить в УК", route: "/contacts" }
]);
```

### Изменение количества отображаемых уведомлений

```jsx
{notifications.slice(0, 2).map(notification => ...)}
// Измените 2 на нужное число
```

## 🐛 Решение проблем

### Tailwind стили не применяются

1. Проверьте `tailwind.config.js` — путь к файлам должен включать ваши компоненты
2. Убедитесь, что импортировали Tailwind CSS в `index.css`
3. Перезапустите dev-сервер

### Компонент не рендерится

1. Проверьте импорт React: `import React from 'react';`
2. Убедитесь, что экспортировали компонент: `export default HomeScreen;`
3. Проверьте консоль браузера на наличие ошибок

## 📞 Контакты

Диспетчерская служба УК "Зелёная долина":
- **Телефон**: [8 (960) 072-03-21](tel:+79600720321)
- **Режим работы**: 24/7

## 📄 Лицензия

© 2024 УК "Зелёная долина". Все права защищены.

---

**Версия**: 1.0.0  
**Дата создания**: Декабрь 2024  
**Технологии**: React 18+, Tailwind CSS 3+
